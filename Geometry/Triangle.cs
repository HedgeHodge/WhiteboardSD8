using System;

namespace Geometry
{
    public class Triangle
    {
        public static double AreaOfTriangle (double triangleBase, double triangleHeight) => (triangleBase * triangleHeight) / 2.0;
    }
}
